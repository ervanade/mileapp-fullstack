module.exports = (router) => {
    /**
     * @swagger
     * tags:
     *   name: Tasks
     *   description: Task management endpoints
     */

    // Add all CRUD operations as swagger comments here
};
